==========================================
🚀 LỆNH KIỂM TRA LOG KHI SUBMIT CODE
==========================================

1. XEM LOG CELERY WORKER (NƠI CHẠY JUDGE_TASK)
   ──────────────────────────────────────────
   Windows PowerShell:
   docker logs -f codebattle-worker-1
   
   Linux/Mac:
   docker logs -f codebattle-worker-1

2. XEM LOG JUDGE0 SERVER (API)
   ───────────────────────────
   docker logs -f codebattle-judge0_server

3. XEM LOG JUDGE0 WORKER (NƠI THỰC SỰ CHẠY CODE)
   ─────────────────────────────────────────────
   docker logs -f codebattle-judge0_worker

4. XEM LOG BACKEND (DJANGO)
   ─────────────────────────
   docker logs -f codebattle-backend-1

5. XEM LOG CÓ FILTER (CHỈ LOG LIÊN QUAN ĐẾN SUBMISSION)
   ────────────────────────────────────────────────────
   Windows PowerShell:
   docker logs -f codebattle-worker-1 2>&1 | Select-String -Pattern "JUDGE|submission|test case"
   
   Linux/Mac:
   docker logs -f codebattle-worker-1 2>&1 | grep -E "JUDGE|submission|test case"

6. XEM LOG 50 DÒNG GẦN NHẤT
   ─────────────────────────
   docker logs --tail=50 codebattle-worker-1

7. XEM TẤT CẢ CONTAINERS ĐANG CHẠY
   ────────────────────────────────
   docker ps

==========================================
📋 CÁC LOG QUAN TRỌNG CẦN TÌM:
==========================================

✅ [JUDGE TASK] Starting judgment for Submission #X
✅ [JUDGE TASK] Found X test cases
✅ [JUDGE TASK] Running test case X/Y
✅ [JUDGE0] Sending submission to ...
✅ [JUDGE0] Received response:
✅ YES hoặc ❌ NO (kết quả test case)
✅ [JUDGE TASK] Judgment completed

==========================================
🎯 CÁCH SỬ DỤNG:
==========================================

1. Mở terminal/PowerShell
2. Chạy một trong các lệnh trên
3. Submit code từ frontend
4. Xem log hiển thị realtime
5. Nhấn Ctrl+C để dừng xem log

==========================================
⚠️ LƯU Ý:
==========================================

- Tên container có thể khác nhau, kiểm tra bằng: docker ps
- Dùng -f để xem log realtime (theo dõi)
- Dùng --tail=N để xem N dòng log gần nhất
- Filter log bằng grep hoặc Select-String để tìm thông tin cụ thể

